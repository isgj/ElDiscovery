<%= form_for(@partecipante) do |f| %>
  <% if @partecipante.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@partecipante.errors.count, "error") %> prohibited this partecipante from being saved:</h2>

      <ul>
      <% @partecipante.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :percorso %><br>
    <%= f.text_field :percorso %>
  </div>
  <div class="field">
    <%= f.label :utente %><br>
    <%= f.text_field :utente %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
